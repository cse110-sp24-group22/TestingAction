/**
 * I am happy with JS doc
 * @param {*} journal
 * @param {*} number
 */
function add(journal, number) {}

/**
 * I am happy with JS doc
 * @param {*} journal
 * @param {*} number
 */
function mul(journal, number) {}
